import React from 'react';
import {Text, View} from 'react-native';

const ParkingTab = (props) => {
    return (
        <View>
            <Text>ParkingTab</Text>
        </View>
    );
};

export default ParkingTab;
